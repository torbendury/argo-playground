server:
  extraArgs: #? Let argo generate TLS certs instead of relying on external LB
    - --insecure
repoServer:
  readinessProbe:
    initialDelaySeconds: 30
  livenessProbe:
    initialDelaySeconds: 30
